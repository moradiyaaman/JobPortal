@{
    ViewData["Title"] = "Access denied";
}
<section class="mx-auto w-full max-w-lg space-y-6 text-center">
    <div class="card space-y-4 p-10">
        <div class="mx-auto flex h-12 w-12 items-center justify-center rounded-full bg-red-100">
            <span class="text-2xl text-red-500">!</span>
        </div>
        <h1 class="text-3xl font-semibold text-slate-900">Access denied</h1>
        <p class="text-sm text-slate-500">You don't have permission to reach this page.</p>
        <div class="pt-2">
            <a class="btn btn-primary" asp-controller="Home" asp-action="Index">Back to home</a>
        </div>
    </div>
</section>
